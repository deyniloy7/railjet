import { SeatHighlightDirective } from './seat-highlight.directive';

describe('SeatHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new SeatHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
